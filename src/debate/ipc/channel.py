"""IPC channel placeholder.

In later stages this module will expose `send` / `recv` primitives
that the orchestrator uses to pass `Message` objects between agents.
"""

from __future__ import annotations
